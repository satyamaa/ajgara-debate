# Ajgara Debate — MVP

## Run locally
1. Install Node.js 20+.
2. Open this folder in a terminal.
3. Run `npm install`
4. Run `npm run dev`
5. Open http://localhost:3000

This is the first visual MVP. The next development phase should add PostgreSQL, authentication and the admin CMS before production deployment.
